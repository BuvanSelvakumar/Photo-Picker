{
  "name": "Photo Picker",
  "productName": "Photo Picker",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package:win": "electron-packager . \"Photo Picker\" --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico",
    "package:mac": "electron-packager . MyApp --platform=darwin --arch=x64 --out=dist --overwrite --icon=assets/icon.icns",
    "package:linux": "electron-packager . MyApp --platform=linux --arch=x64 --out=dist --overwrite --icon=assets/icon.png",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^31.1.0",
    "electron-packager": "^17.1.2"
  }
}
